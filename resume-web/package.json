{
  "name": "resume",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "react-scripts": "1.1.1"
  },
  "dependencies": {
    "@material-ui/icons": "^1.0.0-beta.43",
    "js-beautify": "^1.7.5",
    "material-ui": "^1.0.0-beta.47",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "redux": "^3.7.2"
  },
  "scripts": {
    "start": "cd ../common/bin &&  npm link && cd - && npm link orderDates && react-scripts start",
    "build": "cd ../common/bin &&  npm link && cd - && npm link orderDates && react-scripts build",
    "test": "cd ../common/bin &&  npm link && cd - && npm link orderDates && react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}

